// Schedule Index
%employee_body#shifts
    %table
        %thead
            %tr
                %th Shift Type
                %th Monday
                %th Tuesday
                %th Wednesday
                %th Thursday
                %th Friday
                %th Saturday
                %th Sunday
            %tbody
                %tr
                    %td Manager
                    -   Shift.get_shifts("Manager","Lunch").each do |shift|
                        = render :partial => 'button', locals: {shift: shift}
                %tr
                    %td In-Store 1
                    -   Shift.get_shifts("In_1","Lunch").each do |shift|
                        = render :partial => 'button', locals: {shift: shift}
                %tr
                    %td In-Store 2
                    -   Shift.get_shifts("In_2","Lunch").each do |shift|
                        = render :partial => 'button', locals: {shift: shift}
                %tr
                    %td Driver
                    -   Shift.get_shifts("Driver_1","Lunch").each do |shift|
                        = render :partial => 'button', locals: {shift: shift}
                        
                %tr#gap
                    %td
                    %td
                    %td
                    %td
                    %td
                    %td
                    %td
                    %td
                        
                %tr
                    %td Manager
                    -   Shift.get_shifts("Manager","Dinner").each do |shift|
                        = render :partial => 'button', locals: {shift: shift}
                %tr
                    %td In-Store 1
                    -   Shift.get_shifts("In_1","Dinner").each do |shift|
                        = render :partial => 'button', locals: {shift: shift}
                %tr
                    %td In-Store 2
                    -   Shift.get_shifts("In_2","Dinner").each do |shift|
                        = render :partial => 'button', locals: {shift: shift}
                %tr
                    %td In-Store 3
                    -   Shift.get_shifts("In_3","Dinner").each do |shift|
                        = render :partial => 'button', locals: {shift: shift}
                %tr
                    %td Driver 1
                    -   Shift.get_shifts("Driver_1","Dinner").each do |shift|
                        = render :partial => 'button', locals: {shift: shift}
                %tr
                    %td Driver 2
                    -   Shift.get_shifts("Driver_2","Dinner").each do |shift|
                        = render :partial => 'button', locals: {shift: shift}